recover
 * 변경할 파일이 현재 백업 리스트에 존재한다면 백업 수행 종료 후 복구 진행
  -> '백업 수행 종료'는 remove를 의미? -> backup_list에서 제거하는 것을 의미한다고 가정함... (Q&A에서 발견)

 * 변경할 파일에 대한 백업 파일이 존재하지 않으면 에러 처리 후 프롬포트로 제어가 넘어감

  -> if 변경할 파일이 현재 백업 리스트에 존재하고, 백업 파일이 존재하지 않으면,
        1. 백업 수행 종료 후 / 에러 처리 후 프롬포트로 제어를 넘긴다. -> 이거대로 진행중...
	2. 에러 처리 후 프롬포트로 제어를 넘긴다.