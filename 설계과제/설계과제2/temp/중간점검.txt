1,3 끝내고 중간점검

Systme.out.printf( 가 있는가?

.nextInt()가 있는가?

public class가 있는가? -> 있으면 .c 파일을 생성
분기 -> 만약 file_flag = 1일 경우, 기존 파일을 닫고, 새 파일을 생성하자.

public static void main이 있는가? -> 있으면 main함수 생성, main_flag = 1로 만든다.

import가 있는가? -> 무시
Scanner %s = new Scanner가 있는가? 무시


File 자료형이 있는가?

.write
.flush
.close

main 함수에서 등장하는 return;